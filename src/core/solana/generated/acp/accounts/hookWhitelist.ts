/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const HOOK_WHITELIST_DISCRIMINATOR = new Uint8Array([
  5, 224, 69, 103, 41, 89, 183, 3,
]);

export function getHookWhitelistDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    HOOK_WHITELIST_DISCRIMINATOR,
  );
}

export type HookWhitelist = {
  discriminator: ReadonlyUint8Array;
  /** The whitelisted hook program ID. */
  hook: Address;
  /** PDA bump seed. */
  bump: number;
};

export type HookWhitelistArgs = {
  /** The whitelisted hook program ID. */
  hook: Address;
  /** PDA bump seed. */
  bump: number;
};

/** Gets the encoder for {@link HookWhitelistArgs} account data. */
export function getHookWhitelistEncoder(): FixedSizeEncoder<HookWhitelistArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["hook", getAddressEncoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: HOOK_WHITELIST_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link HookWhitelist} account data. */
export function getHookWhitelistDecoder(): FixedSizeDecoder<HookWhitelist> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["hook", getAddressDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link HookWhitelist} account data. */
export function getHookWhitelistCodec(): FixedSizeCodec<
  HookWhitelistArgs,
  HookWhitelist
> {
  return combineCodec(getHookWhitelistEncoder(), getHookWhitelistDecoder());
}

export function decodeHookWhitelist<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<HookWhitelist, TAddress>;
export function decodeHookWhitelist<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<HookWhitelist, TAddress>;
export function decodeHookWhitelist<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<HookWhitelist, TAddress> | MaybeAccount<HookWhitelist, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getHookWhitelistDecoder(),
  );
}

export async function fetchHookWhitelist<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<HookWhitelist, TAddress>> {
  const maybeAccount = await fetchMaybeHookWhitelist(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeHookWhitelist<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<HookWhitelist, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeHookWhitelist(maybeAccount);
}

export async function fetchAllHookWhitelist(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<HookWhitelist>[]> {
  const maybeAccounts = await fetchAllMaybeHookWhitelist(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeHookWhitelist(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<HookWhitelist>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeHookWhitelist(maybeAccount));
}

export function getHookWhitelistSize(): number {
  return 41;
}
