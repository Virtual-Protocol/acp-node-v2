/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const ACP_STATE_DISCRIMINATOR = new Uint8Array([
  182, 32, 157, 40, 114, 224, 13, 39,
]);

export function getAcpStateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ACP_STATE_DISCRIMINATOR);
}

export type AcpState = {
  discriminator: ReadonlyUint8Array;
  /** Current program authority; can create/remove hook whitelists and update fees. */
  authority: Address;
  /** Nominated successor (two-step transfer). `None` until `nominate_authority` is called. */
  pendingAuthority: Option<Address>;
  /** SPL mint accepted for job budgets. */
  paymentToken: Address;
  /** Wallet that receives platform fees and reclaimed vault rent. */
  platformTreasury: Address;
  /** Platform fee in basis points (0-10000). Applied to vault balance on completion. */
  platformFeeBp: bigint;
  /** Evaluator fee in basis points (0-10000). Applied on evaluator-confirmed completions. */
  evaluatorFeeBp: bigint;
  /** Monotonically increasing counter; next job receives this ID. */
  jobCounter: bigint;
  /** PDA bump seed. */
  bump: number;
};

export type AcpStateArgs = {
  /** Current program authority; can create/remove hook whitelists and update fees. */
  authority: Address;
  /** Nominated successor (two-step transfer). `None` until `nominate_authority` is called. */
  pendingAuthority: OptionOrNullable<Address>;
  /** SPL mint accepted for job budgets. */
  paymentToken: Address;
  /** Wallet that receives platform fees and reclaimed vault rent. */
  platformTreasury: Address;
  /** Platform fee in basis points (0-10000). Applied to vault balance on completion. */
  platformFeeBp: number | bigint;
  /** Evaluator fee in basis points (0-10000). Applied on evaluator-confirmed completions. */
  evaluatorFeeBp: number | bigint;
  /** Monotonically increasing counter; next job receives this ID. */
  jobCounter: number | bigint;
  /** PDA bump seed. */
  bump: number;
};

/** Gets the encoder for {@link AcpStateArgs} account data. */
export function getAcpStateEncoder(): Encoder<AcpStateArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["pendingAuthority", getOptionEncoder(getAddressEncoder())],
      ["paymentToken", getAddressEncoder()],
      ["platformTreasury", getAddressEncoder()],
      ["platformFeeBp", getU64Encoder()],
      ["evaluatorFeeBp", getU64Encoder()],
      ["jobCounter", getU64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ACP_STATE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link AcpState} account data. */
export function getAcpStateDecoder(): Decoder<AcpState> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["pendingAuthority", getOptionDecoder(getAddressDecoder())],
    ["paymentToken", getAddressDecoder()],
    ["platformTreasury", getAddressDecoder()],
    ["platformFeeBp", getU64Decoder()],
    ["evaluatorFeeBp", getU64Decoder()],
    ["jobCounter", getU64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link AcpState} account data. */
export function getAcpStateCodec(): Codec<AcpStateArgs, AcpState> {
  return combineCodec(getAcpStateEncoder(), getAcpStateDecoder());
}

export function decodeAcpState<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<AcpState, TAddress>;
export function decodeAcpState<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<AcpState, TAddress>;
export function decodeAcpState<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<AcpState, TAddress> | MaybeAccount<AcpState, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getAcpStateDecoder(),
  );
}

export async function fetchAcpState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<AcpState, TAddress>> {
  const maybeAccount = await fetchMaybeAcpState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeAcpState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<AcpState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeAcpState(maybeAccount);
}

export async function fetchAllAcpState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<AcpState>[]> {
  const maybeAccounts = await fetchAllMaybeAcpState(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeAcpState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<AcpState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeAcpState(maybeAccount));
}
