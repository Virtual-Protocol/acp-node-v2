/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { AGENTIC_COMMERCE_HOOKED_PROGRAM_ADDRESS } from "../programs/index";

export const ADD_HOOK_WHITELIST_DISCRIMINATOR = new Uint8Array([
  155, 81, 232, 227, 41, 160, 231, 189,
]);

export function getAddHookWhitelistDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADD_HOOK_WHITELIST_DISCRIMINATOR,
  );
}

export type AddHookWhitelistInstruction<
  TProgram extends string = typeof AGENTIC_COMMERCE_HOOKED_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAcpState extends string | AccountMeta<string> = string,
  TAccountHookWhitelist extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAcpState extends string
        ? ReadonlyAccount<TAccountAcpState>
        : TAccountAcpState,
      TAccountHookWhitelist extends string
        ? WritableAccount<TAccountHookWhitelist>
        : TAccountHookWhitelist,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddHookWhitelistInstructionData = {
  discriminator: ReadonlyUint8Array;
  hook: Address;
};

export type AddHookWhitelistInstructionDataArgs = { hook: Address };

export function getAddHookWhitelistInstructionDataEncoder(): FixedSizeEncoder<AddHookWhitelistInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["hook", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: ADD_HOOK_WHITELIST_DISCRIMINATOR }),
  );
}

export function getAddHookWhitelistInstructionDataDecoder(): FixedSizeDecoder<AddHookWhitelistInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["hook", getAddressDecoder()],
  ]);
}

export function getAddHookWhitelistInstructionDataCodec(): FixedSizeCodec<
  AddHookWhitelistInstructionDataArgs,
  AddHookWhitelistInstructionData
> {
  return combineCodec(
    getAddHookWhitelistInstructionDataEncoder(),
    getAddHookWhitelistInstructionDataDecoder(),
  );
}

export type AddHookWhitelistAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAcpState extends string = string,
  TAccountHookWhitelist extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  acpState?: Address<TAccountAcpState>;
  hookWhitelist?: Address<TAccountHookWhitelist>;
  systemProgram?: Address<TAccountSystemProgram>;
  hook: AddHookWhitelistInstructionDataArgs["hook"];
};

export async function getAddHookWhitelistInstructionAsync<
  TAccountAuthority extends string,
  TAccountAcpState extends string,
  TAccountHookWhitelist extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address =
    typeof AGENTIC_COMMERCE_HOOKED_PROGRAM_ADDRESS,
>(
  input: AddHookWhitelistAsyncInput<
    TAccountAuthority,
    TAccountAcpState,
    TAccountHookWhitelist,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AddHookWhitelistInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAcpState,
    TAccountHookWhitelist,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENTIC_COMMERCE_HOOKED_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    acpState: { value: input.acpState ?? null, isWritable: false },
    hookWhitelist: { value: input.hookWhitelist ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.acpState.value) {
    accounts.acpState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([97, 99, 112, 95, 115, 116, 97, 116, 101]),
        ),
      ],
    });
  }
  if (!accounts.hookWhitelist.value) {
    accounts.hookWhitelist.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            104, 111, 111, 107, 95, 119, 104, 105, 116, 101, 108, 105, 115, 116,
          ]),
        ),
        getAddressEncoder().encode(
          getNonNullResolvedInstructionInput("hook", args.hook),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("acpState", accounts.acpState),
      getAccountMeta("hookWhitelist", accounts.hookWhitelist),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAddHookWhitelistInstructionDataEncoder().encode(
      args as AddHookWhitelistInstructionDataArgs,
    ),
    programAddress,
  } as AddHookWhitelistInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAcpState,
    TAccountHookWhitelist,
    TAccountSystemProgram
  >);
}

export type AddHookWhitelistInput<
  TAccountAuthority extends string = string,
  TAccountAcpState extends string = string,
  TAccountHookWhitelist extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  acpState: Address<TAccountAcpState>;
  hookWhitelist: Address<TAccountHookWhitelist>;
  systemProgram?: Address<TAccountSystemProgram>;
  hook: AddHookWhitelistInstructionDataArgs["hook"];
};

export function getAddHookWhitelistInstruction<
  TAccountAuthority extends string,
  TAccountAcpState extends string,
  TAccountHookWhitelist extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address =
    typeof AGENTIC_COMMERCE_HOOKED_PROGRAM_ADDRESS,
>(
  input: AddHookWhitelistInput<
    TAccountAuthority,
    TAccountAcpState,
    TAccountHookWhitelist,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AddHookWhitelistInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAcpState,
  TAccountHookWhitelist,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGENTIC_COMMERCE_HOOKED_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    acpState: { value: input.acpState ?? null, isWritable: false },
    hookWhitelist: { value: input.hookWhitelist ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("acpState", accounts.acpState),
      getAccountMeta("hookWhitelist", accounts.hookWhitelist),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAddHookWhitelistInstructionDataEncoder().encode(
      args as AddHookWhitelistInstructionDataArgs,
    ),
    programAddress,
  } as AddHookWhitelistInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAcpState,
    TAccountHookWhitelist,
    TAccountSystemProgram
  >);
}

export type ParsedAddHookWhitelistInstruction<
  TProgram extends string = typeof AGENTIC_COMMERCE_HOOKED_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    acpState: TAccountMetas[1];
    hookWhitelist: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: AddHookWhitelistInstructionData;
};

export function parseAddHookWhitelistInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddHookWhitelistInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      acpState: getNextAccount(),
      hookWhitelist: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddHookWhitelistInstructionDataDecoder().decode(instruction.data),
  };
}
