/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findHookStatePda } from "../pdas/index.js";
import { FUND_TRANSFER_HOOK_PROGRAM_ADDRESS } from "../programs/index.js";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared/index.js";

export const PRE_CREATE_INTENT_DISCRIMINATOR = new Uint8Array([
  157, 244, 37, 181, 86, 92, 73, 162,
]);

export function getPreCreateIntentDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PRE_CREATE_INTENT_DISCRIMINATOR,
  );
}

export type PreCreateIntentInstruction<
  TProgram extends string = typeof FUND_TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountHookState extends string | AccountMeta<string> = string,
  TAccountJob extends string | AccountMeta<string> = string,
  TAccountIntent extends string | AccountMeta<string> = string,
  TAccountIntentMap extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountHookState extends string
        ? ReadonlyAccount<TAccountHookState>
        : TAccountHookState,
      TAccountJob extends string ? ReadonlyAccount<TAccountJob> : TAccountJob,
      TAccountIntent extends string
        ? WritableAccount<TAccountIntent>
        : TAccountIntent,
      TAccountIntentMap extends string
        ? WritableAccount<TAccountIntentMap>
        : TAccountIntentMap,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PreCreateIntentInstructionData = {
  discriminator: ReadonlyUint8Array;
  jobKey: Address;
  kind: number;
};

export type PreCreateIntentInstructionDataArgs = {
  jobKey: Address;
  kind: number;
};

export function getPreCreateIntentInstructionDataEncoder(): FixedSizeEncoder<PreCreateIntentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["jobKey", getAddressEncoder()],
      ["kind", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PRE_CREATE_INTENT_DISCRIMINATOR }),
  );
}

export function getPreCreateIntentInstructionDataDecoder(): FixedSizeDecoder<PreCreateIntentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["jobKey", getAddressDecoder()],
    ["kind", getU8Decoder()],
  ]);
}

export function getPreCreateIntentInstructionDataCodec(): FixedSizeCodec<
  PreCreateIntentInstructionDataArgs,
  PreCreateIntentInstructionData
> {
  return combineCodec(
    getPreCreateIntentInstructionDataEncoder(),
    getPreCreateIntentInstructionDataDecoder(),
  );
}

export type PreCreateIntentAsyncInput<
  TAccountPayer extends string = string,
  TAccountHookState extends string = string,
  TAccountJob extends string = string,
  TAccountIntent extends string = string,
  TAccountIntentMap extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Rent payer; must be the job's provider (the wallet the lifecycle
   * handlers debit, and the wallet the paymaster prefunds).
   */
  payer: TransactionSigner<TAccountPayer>;
  hookState?: Address<TAccountHookState>;
  /** via `deserialize_job` against `hook_state.acp_program`. */
  job: Address<TAccountJob>;
  intent: Address<TAccountIntent>;
  /** (seed prefix depends on `kind`). */
  intentMap: Address<TAccountIntentMap>;
  systemProgram?: Address<TAccountSystemProgram>;
  jobKey: PreCreateIntentInstructionDataArgs["jobKey"];
  kind: PreCreateIntentInstructionDataArgs["kind"];
};

export async function getPreCreateIntentInstructionAsync<
  TAccountPayer extends string,
  TAccountHookState extends string,
  TAccountJob extends string,
  TAccountIntent extends string,
  TAccountIntentMap extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FUND_TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: PreCreateIntentAsyncInput<
    TAccountPayer,
    TAccountHookState,
    TAccountJob,
    TAccountIntent,
    TAccountIntentMap,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PreCreateIntentInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountHookState,
    TAccountJob,
    TAccountIntent,
    TAccountIntentMap,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FUND_TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    hookState: { value: input.hookState ?? null, isWritable: false },
    job: { value: input.job ?? null, isWritable: false },
    intent: { value: input.intent ?? null, isWritable: true },
    intentMap: { value: input.intentMap ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hookState.value) {
    accounts.hookState.value = await findHookStatePda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.hookState),
      getAccountMeta(accounts.job),
      getAccountMeta(accounts.intent),
      getAccountMeta(accounts.intentMap),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getPreCreateIntentInstructionDataEncoder().encode(
      args as PreCreateIntentInstructionDataArgs,
    ),
    programAddress,
  } as PreCreateIntentInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountHookState,
    TAccountJob,
    TAccountIntent,
    TAccountIntentMap,
    TAccountSystemProgram
  >);
}

export type PreCreateIntentInput<
  TAccountPayer extends string = string,
  TAccountHookState extends string = string,
  TAccountJob extends string = string,
  TAccountIntent extends string = string,
  TAccountIntentMap extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Rent payer; must be the job's provider (the wallet the lifecycle
   * handlers debit, and the wallet the paymaster prefunds).
   */
  payer: TransactionSigner<TAccountPayer>;
  hookState: Address<TAccountHookState>;
  /** via `deserialize_job` against `hook_state.acp_program`. */
  job: Address<TAccountJob>;
  intent: Address<TAccountIntent>;
  /** (seed prefix depends on `kind`). */
  intentMap: Address<TAccountIntentMap>;
  systemProgram?: Address<TAccountSystemProgram>;
  jobKey: PreCreateIntentInstructionDataArgs["jobKey"];
  kind: PreCreateIntentInstructionDataArgs["kind"];
};

export function getPreCreateIntentInstruction<
  TAccountPayer extends string,
  TAccountHookState extends string,
  TAccountJob extends string,
  TAccountIntent extends string,
  TAccountIntentMap extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FUND_TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: PreCreateIntentInput<
    TAccountPayer,
    TAccountHookState,
    TAccountJob,
    TAccountIntent,
    TAccountIntentMap,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PreCreateIntentInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountHookState,
  TAccountJob,
  TAccountIntent,
  TAccountIntentMap,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FUND_TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    hookState: { value: input.hookState ?? null, isWritable: false },
    job: { value: input.job ?? null, isWritable: false },
    intent: { value: input.intent ?? null, isWritable: true },
    intentMap: { value: input.intentMap ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.hookState),
      getAccountMeta(accounts.job),
      getAccountMeta(accounts.intent),
      getAccountMeta(accounts.intentMap),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getPreCreateIntentInstructionDataEncoder().encode(
      args as PreCreateIntentInstructionDataArgs,
    ),
    programAddress,
  } as PreCreateIntentInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountHookState,
    TAccountJob,
    TAccountIntent,
    TAccountIntentMap,
    TAccountSystemProgram
  >);
}

export type ParsedPreCreateIntentInstruction<
  TProgram extends string = typeof FUND_TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Rent payer; must be the job's provider (the wallet the lifecycle
     * handlers debit, and the wallet the paymaster prefunds).
     */
    payer: TAccountMetas[0];
    hookState: TAccountMetas[1];
    /** via `deserialize_job` against `hook_state.acp_program`. */
    job: TAccountMetas[2];
    intent: TAccountMetas[3];
    /** (seed prefix depends on `kind`). */
    intentMap: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: PreCreateIntentInstructionData;
};

export function parsePreCreateIntentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPreCreateIntentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      hookState: getNextAccount(),
      job: getNextAccount(),
      intent: getNextAccount(),
      intentMap: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getPreCreateIntentInstructionDataDecoder().decode(instruction.data),
  };
}
