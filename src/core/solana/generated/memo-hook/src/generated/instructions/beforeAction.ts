/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { MEMO_HOOK_PROGRAM_ADDRESS } from "../programs/index";

export const BEFORE_ACTION_DISCRIMINATOR = new Uint8Array([
  5, 61, 71, 243, 197, 108, 160, 137,
]);

export function getBeforeActionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    BEFORE_ACTION_DISCRIMINATOR,
  );
}

export type BeforeActionInstruction<
  TProgram extends string = typeof MEMO_HOOK_PROGRAM_ADDRESS,
  TAccountHookState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountHookState extends string
        ? ReadonlyAccount<TAccountHookState>
        : TAccountHookState,
      ...TRemainingAccounts,
    ]
  >;

export type BeforeActionInstructionData = {
  discriminator: ReadonlyUint8Array;
  jobId: bigint;
  action: number;
};

export type BeforeActionInstructionDataArgs = {
  jobId: number | bigint;
  action: number;
};

export function getBeforeActionInstructionDataEncoder(): FixedSizeEncoder<BeforeActionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["jobId", getU64Encoder()],
      ["action", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: BEFORE_ACTION_DISCRIMINATOR }),
  );
}

export function getBeforeActionInstructionDataDecoder(): FixedSizeDecoder<BeforeActionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["jobId", getU64Decoder()],
    ["action", getU8Decoder()],
  ]);
}

export function getBeforeActionInstructionDataCodec(): FixedSizeCodec<
  BeforeActionInstructionDataArgs,
  BeforeActionInstructionData
> {
  return combineCodec(
    getBeforeActionInstructionDataEncoder(),
    getBeforeActionInstructionDataDecoder(),
  );
}

export type BeforeActionAsyncInput<TAccountHookState extends string = string> =
  {
    hookState?: Address<TAccountHookState>;
    jobId: BeforeActionInstructionDataArgs["jobId"];
    action: BeforeActionInstructionDataArgs["action"];
  };

export async function getBeforeActionInstructionAsync<
  TAccountHookState extends string,
  TProgramAddress extends Address = typeof MEMO_HOOK_PROGRAM_ADDRESS,
>(
  input: BeforeActionAsyncInput<TAccountHookState>,
  config?: { programAddress?: TProgramAddress },
): Promise<BeforeActionInstruction<TProgramAddress, TAccountHookState>> {
  // Program address.
  const programAddress = config?.programAddress ?? MEMO_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    hookState: { value: input.hookState ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hookState.value) {
    accounts.hookState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([104, 111, 111, 107, 95, 115, 116, 97, 116, 101]),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("hookState", accounts.hookState)],
    data: getBeforeActionInstructionDataEncoder().encode(
      args as BeforeActionInstructionDataArgs,
    ),
    programAddress,
  } as BeforeActionInstruction<TProgramAddress, TAccountHookState>);
}

export type BeforeActionInput<TAccountHookState extends string = string> = {
  hookState: Address<TAccountHookState>;
  jobId: BeforeActionInstructionDataArgs["jobId"];
  action: BeforeActionInstructionDataArgs["action"];
};

export function getBeforeActionInstruction<
  TAccountHookState extends string,
  TProgramAddress extends Address = typeof MEMO_HOOK_PROGRAM_ADDRESS,
>(
  input: BeforeActionInput<TAccountHookState>,
  config?: { programAddress?: TProgramAddress },
): BeforeActionInstruction<TProgramAddress, TAccountHookState> {
  // Program address.
  const programAddress = config?.programAddress ?? MEMO_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    hookState: { value: input.hookState ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("hookState", accounts.hookState)],
    data: getBeforeActionInstructionDataEncoder().encode(
      args as BeforeActionInstructionDataArgs,
    ),
    programAddress,
  } as BeforeActionInstruction<TProgramAddress, TAccountHookState>);
}

export type ParsedBeforeActionInstruction<
  TProgram extends string = typeof MEMO_HOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    hookState: TAccountMetas[0];
  };
  data: BeforeActionInstructionData;
};

export function parseBeforeActionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedBeforeActionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { hookState: getNextAccount() },
    data: getBeforeActionInstructionDataDecoder().decode(instruction.data),
  };
}
