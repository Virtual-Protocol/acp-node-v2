/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getCachedHookMetadataDecoder,
  getCachedHookMetadataEncoder,
  type CachedHookMetadata,
  type CachedHookMetadataArgs,
} from "../types/index.js";

export const HOOK_ROUTER_DISCRIMINATOR = new Uint8Array([
  184, 143, 187, 182, 118, 184, 3, 236,
]);

export function getHookRouterDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(HOOK_ROUTER_DISCRIMINATOR);
}

export type HookRouter = {
  discriminator: ReadonlyUint8Array;
  jobId: bigint;
  setBudgetHooks: Array<Address>;
  fundHooks: Array<Address>;
  submitHooks: Array<Address>;
  completeHooks: Array<Address>;
  rejectHooks: Array<Address>;
  /**
   * Cached metadata for unique configured sub-hooks. Used by
   * `validate_completeness` and fund-time re-validation.
   */
  hookMetadataCache: Array<CachedHookMetadata>;
  bump: number;
};

export type HookRouterArgs = {
  jobId: number | bigint;
  setBudgetHooks: Array<Address>;
  fundHooks: Array<Address>;
  submitHooks: Array<Address>;
  completeHooks: Array<Address>;
  rejectHooks: Array<Address>;
  /**
   * Cached metadata for unique configured sub-hooks. Used by
   * `validate_completeness` and fund-time re-validation.
   */
  hookMetadataCache: Array<CachedHookMetadataArgs>;
  bump: number;
};

/** Gets the encoder for {@link HookRouterArgs} account data. */
export function getHookRouterEncoder(): Encoder<HookRouterArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["jobId", getU64Encoder()],
      ["setBudgetHooks", getArrayEncoder(getAddressEncoder())],
      ["fundHooks", getArrayEncoder(getAddressEncoder())],
      ["submitHooks", getArrayEncoder(getAddressEncoder())],
      ["completeHooks", getArrayEncoder(getAddressEncoder())],
      ["rejectHooks", getArrayEncoder(getAddressEncoder())],
      ["hookMetadataCache", getArrayEncoder(getCachedHookMetadataEncoder())],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: HOOK_ROUTER_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link HookRouter} account data. */
export function getHookRouterDecoder(): Decoder<HookRouter> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["jobId", getU64Decoder()],
    ["setBudgetHooks", getArrayDecoder(getAddressDecoder())],
    ["fundHooks", getArrayDecoder(getAddressDecoder())],
    ["submitHooks", getArrayDecoder(getAddressDecoder())],
    ["completeHooks", getArrayDecoder(getAddressDecoder())],
    ["rejectHooks", getArrayDecoder(getAddressDecoder())],
    ["hookMetadataCache", getArrayDecoder(getCachedHookMetadataDecoder())],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link HookRouter} account data. */
export function getHookRouterCodec(): Codec<HookRouterArgs, HookRouter> {
  return combineCodec(getHookRouterEncoder(), getHookRouterDecoder());
}

export function decodeHookRouter<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<HookRouter, TAddress>;
export function decodeHookRouter<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<HookRouter, TAddress>;
export function decodeHookRouter<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<HookRouter, TAddress> | MaybeAccount<HookRouter, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getHookRouterDecoder(),
  );
}

export async function fetchHookRouter<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<HookRouter, TAddress>> {
  const maybeAccount = await fetchMaybeHookRouter(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeHookRouter<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<HookRouter, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeHookRouter(maybeAccount);
}

export async function fetchAllHookRouter(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<HookRouter>[]> {
  const maybeAccounts = await fetchAllMaybeHookRouter(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeHookRouter(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<HookRouter>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeHookRouter(maybeAccount));
}
