/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const ROUTER_STATE_DISCRIMINATOR = new Uint8Array([
  141, 157, 194, 155, 75, 208, 200, 27,
]);

export function getRouterStateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ROUTER_STATE_DISCRIMINATOR,
  );
}

export type RouterState = {
  discriminator: ReadonlyUint8Array;
  acpProgram: Address;
  authority: Address;
  pendingAuthority: Option<Address>;
  /**
   * Admin-configurable max sub-hooks per job, per selector. Range: 1..=255.
   * No logical upper bound (mirrors EVM `MultiHookRouter.maxHooksPerJob`).
   * Practical limit governed by Solana tx-size at runtime (~5-8 hooks per
   * call without Address Lookup Tables).
   */
  maxHooksPerJob: number;
  bump: number;
};

export type RouterStateArgs = {
  acpProgram: Address;
  authority: Address;
  pendingAuthority: OptionOrNullable<Address>;
  /**
   * Admin-configurable max sub-hooks per job, per selector. Range: 1..=255.
   * No logical upper bound (mirrors EVM `MultiHookRouter.maxHooksPerJob`).
   * Practical limit governed by Solana tx-size at runtime (~5-8 hooks per
   * call without Address Lookup Tables).
   */
  maxHooksPerJob: number;
  bump: number;
};

/** Gets the encoder for {@link RouterStateArgs} account data. */
export function getRouterStateEncoder(): Encoder<RouterStateArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["acpProgram", getAddressEncoder()],
      ["authority", getAddressEncoder()],
      ["pendingAuthority", getOptionEncoder(getAddressEncoder())],
      ["maxHooksPerJob", getU8Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ROUTER_STATE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link RouterState} account data. */
export function getRouterStateDecoder(): Decoder<RouterState> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["acpProgram", getAddressDecoder()],
    ["authority", getAddressDecoder()],
    ["pendingAuthority", getOptionDecoder(getAddressDecoder())],
    ["maxHooksPerJob", getU8Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link RouterState} account data. */
export function getRouterStateCodec(): Codec<RouterStateArgs, RouterState> {
  return combineCodec(getRouterStateEncoder(), getRouterStateDecoder());
}

export function decodeRouterState<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<RouterState, TAddress>;
export function decodeRouterState<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<RouterState, TAddress>;
export function decodeRouterState<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<RouterState, TAddress> | MaybeAccount<RouterState, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getRouterStateDecoder(),
  );
}

export async function fetchRouterState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<RouterState, TAddress>> {
  const maybeAccount = await fetchMaybeRouterState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeRouterState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<RouterState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeRouterState(maybeAccount);
}

export async function fetchAllRouterState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<RouterState>[]> {
  const maybeAccounts = await fetchAllMaybeRouterState(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeRouterState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<RouterState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeRouterState(maybeAccount));
}
