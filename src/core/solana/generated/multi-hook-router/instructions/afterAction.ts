/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import { findHookRouterPda, findRouterStatePda } from "../pdas/index.js";
import { MULTI_HOOK_ROUTER_PROGRAM_ADDRESS } from "../programs/index.js";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared/index.js";

export const AFTER_ACTION_DISCRIMINATOR = new Uint8Array([
  100, 247, 61, 195, 161, 140, 250, 73,
]);

export function getAfterActionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    AFTER_ACTION_DISCRIMINATOR,
  );
}

export type AfterActionInstruction<
  TProgram extends string = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
  TAccountHookRouter extends string | AccountMeta<string> = string,
  TAccountRouterState extends string | AccountMeta<string> = string,
  TAccountInstructionsSysvar extends string | AccountMeta<string> =
    "Sysvar1nstructions1111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountHookRouter extends string
        ? ReadonlyAccount<TAccountHookRouter>
        : TAccountHookRouter,
      TAccountRouterState extends string
        ? ReadonlyAccount<TAccountRouterState>
        : TAccountRouterState,
      TAccountInstructionsSysvar extends string
        ? ReadonlyAccount<TAccountInstructionsSysvar>
        : TAccountInstructionsSysvar,
      ...TRemainingAccounts,
    ]
  >;

export type AfterActionInstructionData = {
  discriminator: ReadonlyUint8Array;
  jobId: bigint;
  action: number;
  optParams: ReadonlyUint8Array;
};

export type AfterActionInstructionDataArgs = {
  jobId: number | bigint;
  action: number;
  optParams: ReadonlyUint8Array;
};

export function getAfterActionInstructionDataEncoder(): Encoder<AfterActionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["jobId", getU64Encoder()],
      ["action", getU8Encoder()],
      ["optParams", addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: AFTER_ACTION_DISCRIMINATOR }),
  );
}

export function getAfterActionInstructionDataDecoder(): Decoder<AfterActionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["jobId", getU64Decoder()],
    ["action", getU8Decoder()],
    ["optParams", addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
  ]);
}

export function getAfterActionInstructionDataCodec(): Codec<
  AfterActionInstructionDataArgs,
  AfterActionInstructionData
> {
  return combineCodec(
    getAfterActionInstructionDataEncoder(),
    getAfterActionInstructionDataDecoder(),
  );
}

export type AfterActionAsyncInput<
  TAccountHookRouter extends string = string,
  TAccountRouterState extends string = string,
  TAccountInstructionsSysvar extends string = string,
> = {
  /** See comment on BeforeAction.hook_router. */
  hookRouter?: Address<TAccountHookRouter>;
  routerState?: Address<TAccountRouterState>;
  instructionsSysvar?: Address<TAccountInstructionsSysvar>;
  jobId: AfterActionInstructionDataArgs["jobId"];
  action: AfterActionInstructionDataArgs["action"];
  optParams: AfterActionInstructionDataArgs["optParams"];
};

export async function getAfterActionInstructionAsync<
  TAccountHookRouter extends string,
  TAccountRouterState extends string,
  TAccountInstructionsSysvar extends string,
  TProgramAddress extends Address = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
>(
  input: AfterActionAsyncInput<
    TAccountHookRouter,
    TAccountRouterState,
    TAccountInstructionsSysvar
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AfterActionInstruction<
    TProgramAddress,
    TAccountHookRouter,
    TAccountRouterState,
    TAccountInstructionsSysvar
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MULTI_HOOK_ROUTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    hookRouter: { value: input.hookRouter ?? null, isWritable: false },
    routerState: { value: input.routerState ?? null, isWritable: false },
    instructionsSysvar: {
      value: input.instructionsSysvar ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hookRouter.value) {
    accounts.hookRouter.value = await findHookRouterPda({
      jobId: expectSome(args.jobId),
    });
  }
  if (!accounts.routerState.value) {
    accounts.routerState.value = await findRouterStatePda();
  }
  if (!accounts.instructionsSysvar.value) {
    accounts.instructionsSysvar.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.hookRouter),
      getAccountMeta(accounts.routerState),
      getAccountMeta(accounts.instructionsSysvar),
    ],
    data: getAfterActionInstructionDataEncoder().encode(
      args as AfterActionInstructionDataArgs,
    ),
    programAddress,
  } as AfterActionInstruction<
    TProgramAddress,
    TAccountHookRouter,
    TAccountRouterState,
    TAccountInstructionsSysvar
  >);
}

export type AfterActionInput<
  TAccountHookRouter extends string = string,
  TAccountRouterState extends string = string,
  TAccountInstructionsSysvar extends string = string,
> = {
  /** See comment on BeforeAction.hook_router. */
  hookRouter: Address<TAccountHookRouter>;
  routerState: Address<TAccountRouterState>;
  instructionsSysvar?: Address<TAccountInstructionsSysvar>;
  jobId: AfterActionInstructionDataArgs["jobId"];
  action: AfterActionInstructionDataArgs["action"];
  optParams: AfterActionInstructionDataArgs["optParams"];
};

export function getAfterActionInstruction<
  TAccountHookRouter extends string,
  TAccountRouterState extends string,
  TAccountInstructionsSysvar extends string,
  TProgramAddress extends Address = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
>(
  input: AfterActionInput<
    TAccountHookRouter,
    TAccountRouterState,
    TAccountInstructionsSysvar
  >,
  config?: { programAddress?: TProgramAddress },
): AfterActionInstruction<
  TProgramAddress,
  TAccountHookRouter,
  TAccountRouterState,
  TAccountInstructionsSysvar
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MULTI_HOOK_ROUTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    hookRouter: { value: input.hookRouter ?? null, isWritable: false },
    routerState: { value: input.routerState ?? null, isWritable: false },
    instructionsSysvar: {
      value: input.instructionsSysvar ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.instructionsSysvar.value) {
    accounts.instructionsSysvar.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.hookRouter),
      getAccountMeta(accounts.routerState),
      getAccountMeta(accounts.instructionsSysvar),
    ],
    data: getAfterActionInstructionDataEncoder().encode(
      args as AfterActionInstructionDataArgs,
    ),
    programAddress,
  } as AfterActionInstruction<
    TProgramAddress,
    TAccountHookRouter,
    TAccountRouterState,
    TAccountInstructionsSysvar
  >);
}

export type ParsedAfterActionInstruction<
  TProgram extends string = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** See comment on BeforeAction.hook_router. */
    hookRouter: TAccountMetas[0];
    routerState: TAccountMetas[1];
    instructionsSysvar: TAccountMetas[2];
  };
  data: AfterActionInstructionData;
};

export function parseAfterActionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAfterActionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      hookRouter: getNextAccount(),
      routerState: getNextAccount(),
      instructionsSysvar: getNextAccount(),
    },
    data: getAfterActionInstructionDataDecoder().decode(instruction.data),
  };
}
