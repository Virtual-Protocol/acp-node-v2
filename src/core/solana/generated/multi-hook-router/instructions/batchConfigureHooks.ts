/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findHookRouterPda, findRouterStatePda } from "../pdas/index.js";
import { MULTI_HOOK_ROUTER_PROGRAM_ADDRESS } from "../programs/index.js";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared/index.js";

export const BATCH_CONFIGURE_HOOKS_DISCRIMINATOR = new Uint8Array([
  152, 156, 231, 0, 247, 197, 31, 156,
]);

export function getBatchConfigureHooksDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    BATCH_CONFIGURE_HOOKS_DISCRIMINATOR,
  );
}

export type BatchConfigureHooksInstruction<
  TProgram extends string = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
  TAccountClient extends string | AccountMeta<string> = string,
  TAccountJob extends string | AccountMeta<string> = string,
  TAccountHookRouter extends string | AccountMeta<string> = string,
  TAccountRouterState extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountClient extends string
        ? WritableSignerAccount<TAccountClient> &
            AccountSignerMeta<TAccountClient>
        : TAccountClient,
      TAccountJob extends string ? ReadonlyAccount<TAccountJob> : TAccountJob,
      TAccountHookRouter extends string
        ? WritableAccount<TAccountHookRouter>
        : TAccountHookRouter,
      TAccountRouterState extends string
        ? ReadonlyAccount<TAccountRouterState>
        : TAccountRouterState,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type BatchConfigureHooksInstructionData = {
  discriminator: ReadonlyUint8Array;
  jobId: bigint;
  setBudget: Array<Address>;
  fund: Array<Address>;
  submit: Array<Address>;
  complete: Array<Address>;
  reject: Array<Address>;
};

export type BatchConfigureHooksInstructionDataArgs = {
  jobId: number | bigint;
  setBudget: Array<Address>;
  fund: Array<Address>;
  submit: Array<Address>;
  complete: Array<Address>;
  reject: Array<Address>;
};

export function getBatchConfigureHooksInstructionDataEncoder(): Encoder<BatchConfigureHooksInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["jobId", getU64Encoder()],
      ["setBudget", getArrayEncoder(getAddressEncoder())],
      ["fund", getArrayEncoder(getAddressEncoder())],
      ["submit", getArrayEncoder(getAddressEncoder())],
      ["complete", getArrayEncoder(getAddressEncoder())],
      ["reject", getArrayEncoder(getAddressEncoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: BATCH_CONFIGURE_HOOKS_DISCRIMINATOR,
    }),
  );
}

export function getBatchConfigureHooksInstructionDataDecoder(): Decoder<BatchConfigureHooksInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["jobId", getU64Decoder()],
    ["setBudget", getArrayDecoder(getAddressDecoder())],
    ["fund", getArrayDecoder(getAddressDecoder())],
    ["submit", getArrayDecoder(getAddressDecoder())],
    ["complete", getArrayDecoder(getAddressDecoder())],
    ["reject", getArrayDecoder(getAddressDecoder())],
  ]);
}

export function getBatchConfigureHooksInstructionDataCodec(): Codec<
  BatchConfigureHooksInstructionDataArgs,
  BatchConfigureHooksInstructionData
> {
  return combineCodec(
    getBatchConfigureHooksInstructionDataEncoder(),
    getBatchConfigureHooksInstructionDataDecoder(),
  );
}

export type BatchConfigureHooksAsyncInput<
  TAccountClient extends string = string,
  TAccountJob extends string = string,
  TAccountHookRouter extends string = string,
  TAccountRouterState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  client: TransactionSigner<TAccountClient>;
  job: Address<TAccountJob>;
  hookRouter?: Address<TAccountHookRouter>;
  routerState?: Address<TAccountRouterState>;
  systemProgram?: Address<TAccountSystemProgram>;
  jobId: BatchConfigureHooksInstructionDataArgs["jobId"];
  setBudget: BatchConfigureHooksInstructionDataArgs["setBudget"];
  fund: BatchConfigureHooksInstructionDataArgs["fund"];
  submit: BatchConfigureHooksInstructionDataArgs["submit"];
  complete: BatchConfigureHooksInstructionDataArgs["complete"];
  reject: BatchConfigureHooksInstructionDataArgs["reject"];
};

export async function getBatchConfigureHooksInstructionAsync<
  TAccountClient extends string,
  TAccountJob extends string,
  TAccountHookRouter extends string,
  TAccountRouterState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
>(
  input: BatchConfigureHooksAsyncInput<
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  BatchConfigureHooksInstruction<
    TProgramAddress,
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MULTI_HOOK_ROUTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    client: { value: input.client ?? null, isWritable: true },
    job: { value: input.job ?? null, isWritable: false },
    hookRouter: { value: input.hookRouter ?? null, isWritable: true },
    routerState: { value: input.routerState ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hookRouter.value) {
    accounts.hookRouter.value = await findHookRouterPda({
      jobId: expectSome(args.jobId),
    });
  }
  if (!accounts.routerState.value) {
    accounts.routerState.value = await findRouterStatePda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.client),
      getAccountMeta(accounts.job),
      getAccountMeta(accounts.hookRouter),
      getAccountMeta(accounts.routerState),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getBatchConfigureHooksInstructionDataEncoder().encode(
      args as BatchConfigureHooksInstructionDataArgs,
    ),
    programAddress,
  } as BatchConfigureHooksInstruction<
    TProgramAddress,
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState,
    TAccountSystemProgram
  >);
}

export type BatchConfigureHooksInput<
  TAccountClient extends string = string,
  TAccountJob extends string = string,
  TAccountHookRouter extends string = string,
  TAccountRouterState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  client: TransactionSigner<TAccountClient>;
  job: Address<TAccountJob>;
  hookRouter: Address<TAccountHookRouter>;
  routerState: Address<TAccountRouterState>;
  systemProgram?: Address<TAccountSystemProgram>;
  jobId: BatchConfigureHooksInstructionDataArgs["jobId"];
  setBudget: BatchConfigureHooksInstructionDataArgs["setBudget"];
  fund: BatchConfigureHooksInstructionDataArgs["fund"];
  submit: BatchConfigureHooksInstructionDataArgs["submit"];
  complete: BatchConfigureHooksInstructionDataArgs["complete"];
  reject: BatchConfigureHooksInstructionDataArgs["reject"];
};

export function getBatchConfigureHooksInstruction<
  TAccountClient extends string,
  TAccountJob extends string,
  TAccountHookRouter extends string,
  TAccountRouterState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
>(
  input: BatchConfigureHooksInput<
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): BatchConfigureHooksInstruction<
  TProgramAddress,
  TAccountClient,
  TAccountJob,
  TAccountHookRouter,
  TAccountRouterState,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MULTI_HOOK_ROUTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    client: { value: input.client ?? null, isWritable: true },
    job: { value: input.job ?? null, isWritable: false },
    hookRouter: { value: input.hookRouter ?? null, isWritable: true },
    routerState: { value: input.routerState ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.client),
      getAccountMeta(accounts.job),
      getAccountMeta(accounts.hookRouter),
      getAccountMeta(accounts.routerState),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getBatchConfigureHooksInstructionDataEncoder().encode(
      args as BatchConfigureHooksInstructionDataArgs,
    ),
    programAddress,
  } as BatchConfigureHooksInstruction<
    TProgramAddress,
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState,
    TAccountSystemProgram
  >);
}

export type ParsedBatchConfigureHooksInstruction<
  TProgram extends string = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    client: TAccountMetas[0];
    job: TAccountMetas[1];
    hookRouter: TAccountMetas[2];
    routerState: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: BatchConfigureHooksInstructionData;
};

export function parseBatchConfigureHooksInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedBatchConfigureHooksInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      client: getNextAccount(),
      job: getNextAccount(),
      hookRouter: getNextAccount(),
      routerState: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getBatchConfigureHooksInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
