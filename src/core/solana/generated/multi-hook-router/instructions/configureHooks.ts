/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findHookRouterPda, findRouterStatePda } from "../pdas/index.js";
import { MULTI_HOOK_ROUTER_PROGRAM_ADDRESS } from "../programs/index.js";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared/index.js";

export const CONFIGURE_HOOKS_DISCRIMINATOR = new Uint8Array([
  171, 90, 59, 227, 56, 135, 114, 228,
]);

export function getConfigureHooksDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CONFIGURE_HOOKS_DISCRIMINATOR,
  );
}

export type ConfigureHooksInstruction<
  TProgram extends string = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
  TAccountClient extends string | AccountMeta<string> = string,
  TAccountJob extends string | AccountMeta<string> = string,
  TAccountHookRouter extends string | AccountMeta<string> = string,
  TAccountRouterState extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountClient extends string
        ? WritableSignerAccount<TAccountClient> &
            AccountSignerMeta<TAccountClient>
        : TAccountClient,
      TAccountJob extends string ? ReadonlyAccount<TAccountJob> : TAccountJob,
      TAccountHookRouter extends string
        ? WritableAccount<TAccountHookRouter>
        : TAccountHookRouter,
      TAccountRouterState extends string
        ? ReadonlyAccount<TAccountRouterState>
        : TAccountRouterState,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ConfigureHooksInstructionData = {
  discriminator: ReadonlyUint8Array;
  jobId: bigint;
  action: number;
  hooks: Array<Address>;
};

export type ConfigureHooksInstructionDataArgs = {
  jobId: number | bigint;
  action: number;
  hooks: Array<Address>;
};

export function getConfigureHooksInstructionDataEncoder(): Encoder<ConfigureHooksInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["jobId", getU64Encoder()],
      ["action", getU8Encoder()],
      ["hooks", getArrayEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: CONFIGURE_HOOKS_DISCRIMINATOR }),
  );
}

export function getConfigureHooksInstructionDataDecoder(): Decoder<ConfigureHooksInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["jobId", getU64Decoder()],
    ["action", getU8Decoder()],
    ["hooks", getArrayDecoder(getAddressDecoder())],
  ]);
}

export function getConfigureHooksInstructionDataCodec(): Codec<
  ConfigureHooksInstructionDataArgs,
  ConfigureHooksInstructionData
> {
  return combineCodec(
    getConfigureHooksInstructionDataEncoder(),
    getConfigureHooksInstructionDataDecoder(),
  );
}

export type ConfigureHooksAsyncInput<
  TAccountClient extends string = string,
  TAccountJob extends string = string,
  TAccountHookRouter extends string = string,
  TAccountRouterState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  client: TransactionSigner<TAccountClient>;
  job: Address<TAccountJob>;
  hookRouter?: Address<TAccountHookRouter>;
  routerState?: Address<TAccountRouterState>;
  systemProgram?: Address<TAccountSystemProgram>;
  jobId: ConfigureHooksInstructionDataArgs["jobId"];
  action: ConfigureHooksInstructionDataArgs["action"];
  hooks: ConfigureHooksInstructionDataArgs["hooks"];
};

export async function getConfigureHooksInstructionAsync<
  TAccountClient extends string,
  TAccountJob extends string,
  TAccountHookRouter extends string,
  TAccountRouterState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
>(
  input: ConfigureHooksAsyncInput<
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ConfigureHooksInstruction<
    TProgramAddress,
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MULTI_HOOK_ROUTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    client: { value: input.client ?? null, isWritable: true },
    job: { value: input.job ?? null, isWritable: false },
    hookRouter: { value: input.hookRouter ?? null, isWritable: true },
    routerState: { value: input.routerState ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hookRouter.value) {
    accounts.hookRouter.value = await findHookRouterPda({
      jobId: expectSome(args.jobId),
    });
  }
  if (!accounts.routerState.value) {
    accounts.routerState.value = await findRouterStatePda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.client),
      getAccountMeta(accounts.job),
      getAccountMeta(accounts.hookRouter),
      getAccountMeta(accounts.routerState),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getConfigureHooksInstructionDataEncoder().encode(
      args as ConfigureHooksInstructionDataArgs,
    ),
    programAddress,
  } as ConfigureHooksInstruction<
    TProgramAddress,
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState,
    TAccountSystemProgram
  >);
}

export type ConfigureHooksInput<
  TAccountClient extends string = string,
  TAccountJob extends string = string,
  TAccountHookRouter extends string = string,
  TAccountRouterState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  client: TransactionSigner<TAccountClient>;
  job: Address<TAccountJob>;
  hookRouter: Address<TAccountHookRouter>;
  routerState: Address<TAccountRouterState>;
  systemProgram?: Address<TAccountSystemProgram>;
  jobId: ConfigureHooksInstructionDataArgs["jobId"];
  action: ConfigureHooksInstructionDataArgs["action"];
  hooks: ConfigureHooksInstructionDataArgs["hooks"];
};

export function getConfigureHooksInstruction<
  TAccountClient extends string,
  TAccountJob extends string,
  TAccountHookRouter extends string,
  TAccountRouterState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
>(
  input: ConfigureHooksInput<
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ConfigureHooksInstruction<
  TProgramAddress,
  TAccountClient,
  TAccountJob,
  TAccountHookRouter,
  TAccountRouterState,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MULTI_HOOK_ROUTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    client: { value: input.client ?? null, isWritable: true },
    job: { value: input.job ?? null, isWritable: false },
    hookRouter: { value: input.hookRouter ?? null, isWritable: true },
    routerState: { value: input.routerState ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.client),
      getAccountMeta(accounts.job),
      getAccountMeta(accounts.hookRouter),
      getAccountMeta(accounts.routerState),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getConfigureHooksInstructionDataEncoder().encode(
      args as ConfigureHooksInstructionDataArgs,
    ),
    programAddress,
  } as ConfigureHooksInstruction<
    TProgramAddress,
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState,
    TAccountSystemProgram
  >);
}

export type ParsedConfigureHooksInstruction<
  TProgram extends string = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    client: TAccountMetas[0];
    job: TAccountMetas[1];
    hookRouter: TAccountMetas[2];
    routerState: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: ConfigureHooksInstructionData;
};

export function parseConfigureHooksInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedConfigureHooksInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      client: getNextAccount(),
      job: getNextAccount(),
      hookRouter: getNextAccount(),
      routerState: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getConfigureHooksInstructionDataDecoder().decode(instruction.data),
  };
}
