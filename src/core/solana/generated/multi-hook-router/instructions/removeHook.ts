/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findHookRouterPda, findRouterStatePda } from "../pdas/index.js";
import { MULTI_HOOK_ROUTER_PROGRAM_ADDRESS } from "../programs/index.js";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared/index.js";

export const REMOVE_HOOK_DISCRIMINATOR = new Uint8Array([
  190, 124, 75, 130, 220, 82, 21, 208,
]);

export function getRemoveHookDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REMOVE_HOOK_DISCRIMINATOR);
}

export type RemoveHookInstruction<
  TProgram extends string = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
  TAccountClient extends string | AccountMeta<string> = string,
  TAccountJob extends string | AccountMeta<string> = string,
  TAccountHookRouter extends string | AccountMeta<string> = string,
  TAccountRouterState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountClient extends string
        ? ReadonlySignerAccount<TAccountClient> &
            AccountSignerMeta<TAccountClient>
        : TAccountClient,
      TAccountJob extends string ? ReadonlyAccount<TAccountJob> : TAccountJob,
      TAccountHookRouter extends string
        ? WritableAccount<TAccountHookRouter>
        : TAccountHookRouter,
      TAccountRouterState extends string
        ? ReadonlyAccount<TAccountRouterState>
        : TAccountRouterState,
      ...TRemainingAccounts,
    ]
  >;

export type RemoveHookInstructionData = {
  discriminator: ReadonlyUint8Array;
  jobId: bigint;
  action: number;
  hook: Address;
};

export type RemoveHookInstructionDataArgs = {
  jobId: number | bigint;
  action: number;
  hook: Address;
};

export function getRemoveHookInstructionDataEncoder(): FixedSizeEncoder<RemoveHookInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["jobId", getU64Encoder()],
      ["action", getU8Encoder()],
      ["hook", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: REMOVE_HOOK_DISCRIMINATOR }),
  );
}

export function getRemoveHookInstructionDataDecoder(): FixedSizeDecoder<RemoveHookInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["jobId", getU64Decoder()],
    ["action", getU8Decoder()],
    ["hook", getAddressDecoder()],
  ]);
}

export function getRemoveHookInstructionDataCodec(): FixedSizeCodec<
  RemoveHookInstructionDataArgs,
  RemoveHookInstructionData
> {
  return combineCodec(
    getRemoveHookInstructionDataEncoder(),
    getRemoveHookInstructionDataDecoder(),
  );
}

export type RemoveHookAsyncInput<
  TAccountClient extends string = string,
  TAccountJob extends string = string,
  TAccountHookRouter extends string = string,
  TAccountRouterState extends string = string,
> = {
  client: TransactionSigner<TAccountClient>;
  job: Address<TAccountJob>;
  hookRouter?: Address<TAccountHookRouter>;
  routerState?: Address<TAccountRouterState>;
  jobId: RemoveHookInstructionDataArgs["jobId"];
  action: RemoveHookInstructionDataArgs["action"];
  hook: RemoveHookInstructionDataArgs["hook"];
};

export async function getRemoveHookInstructionAsync<
  TAccountClient extends string,
  TAccountJob extends string,
  TAccountHookRouter extends string,
  TAccountRouterState extends string,
  TProgramAddress extends Address = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
>(
  input: RemoveHookAsyncInput<
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RemoveHookInstruction<
    TProgramAddress,
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MULTI_HOOK_ROUTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    client: { value: input.client ?? null, isWritable: false },
    job: { value: input.job ?? null, isWritable: false },
    hookRouter: { value: input.hookRouter ?? null, isWritable: true },
    routerState: { value: input.routerState ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hookRouter.value) {
    accounts.hookRouter.value = await findHookRouterPda({
      jobId: expectSome(args.jobId),
    });
  }
  if (!accounts.routerState.value) {
    accounts.routerState.value = await findRouterStatePda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.client),
      getAccountMeta(accounts.job),
      getAccountMeta(accounts.hookRouter),
      getAccountMeta(accounts.routerState),
    ],
    data: getRemoveHookInstructionDataEncoder().encode(
      args as RemoveHookInstructionDataArgs,
    ),
    programAddress,
  } as RemoveHookInstruction<
    TProgramAddress,
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState
  >);
}

export type RemoveHookInput<
  TAccountClient extends string = string,
  TAccountJob extends string = string,
  TAccountHookRouter extends string = string,
  TAccountRouterState extends string = string,
> = {
  client: TransactionSigner<TAccountClient>;
  job: Address<TAccountJob>;
  hookRouter: Address<TAccountHookRouter>;
  routerState: Address<TAccountRouterState>;
  jobId: RemoveHookInstructionDataArgs["jobId"];
  action: RemoveHookInstructionDataArgs["action"];
  hook: RemoveHookInstructionDataArgs["hook"];
};

export function getRemoveHookInstruction<
  TAccountClient extends string,
  TAccountJob extends string,
  TAccountHookRouter extends string,
  TAccountRouterState extends string,
  TProgramAddress extends Address = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
>(
  input: RemoveHookInput<
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState
  >,
  config?: { programAddress?: TProgramAddress },
): RemoveHookInstruction<
  TProgramAddress,
  TAccountClient,
  TAccountJob,
  TAccountHookRouter,
  TAccountRouterState
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MULTI_HOOK_ROUTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    client: { value: input.client ?? null, isWritable: false },
    job: { value: input.job ?? null, isWritable: false },
    hookRouter: { value: input.hookRouter ?? null, isWritable: true },
    routerState: { value: input.routerState ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.client),
      getAccountMeta(accounts.job),
      getAccountMeta(accounts.hookRouter),
      getAccountMeta(accounts.routerState),
    ],
    data: getRemoveHookInstructionDataEncoder().encode(
      args as RemoveHookInstructionDataArgs,
    ),
    programAddress,
  } as RemoveHookInstruction<
    TProgramAddress,
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState
  >);
}

export type ParsedRemoveHookInstruction<
  TProgram extends string = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    client: TAccountMetas[0];
    job: TAccountMetas[1];
    hookRouter: TAccountMetas[2];
    routerState: TAccountMetas[3];
  };
  data: RemoveHookInstructionData;
};

export function parseRemoveHookInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRemoveHookInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      client: getNextAccount(),
      job: getNextAccount(),
      hookRouter: getNextAccount(),
      routerState: getNextAccount(),
    },
    data: getRemoveHookInstructionDataDecoder().decode(instruction.data),
  };
}
