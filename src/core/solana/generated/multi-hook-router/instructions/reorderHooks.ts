/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findHookRouterPda, findRouterStatePda } from "../pdas/index.js";
import { MULTI_HOOK_ROUTER_PROGRAM_ADDRESS } from "../programs/index.js";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared/index.js";

export const REORDER_HOOKS_DISCRIMINATOR = new Uint8Array([
  206, 139, 158, 238, 151, 152, 40, 246,
]);

export function getReorderHooksDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REORDER_HOOKS_DISCRIMINATOR,
  );
}

export type ReorderHooksInstruction<
  TProgram extends string = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
  TAccountClient extends string | AccountMeta<string> = string,
  TAccountJob extends string | AccountMeta<string> = string,
  TAccountHookRouter extends string | AccountMeta<string> = string,
  TAccountRouterState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountClient extends string
        ? ReadonlySignerAccount<TAccountClient> &
            AccountSignerMeta<TAccountClient>
        : TAccountClient,
      TAccountJob extends string ? ReadonlyAccount<TAccountJob> : TAccountJob,
      TAccountHookRouter extends string
        ? WritableAccount<TAccountHookRouter>
        : TAccountHookRouter,
      TAccountRouterState extends string
        ? ReadonlyAccount<TAccountRouterState>
        : TAccountRouterState,
      ...TRemainingAccounts,
    ]
  >;

export type ReorderHooksInstructionData = {
  discriminator: ReadonlyUint8Array;
  jobId: bigint;
  action: number;
  hooks: Array<Address>;
};

export type ReorderHooksInstructionDataArgs = {
  jobId: number | bigint;
  action: number;
  hooks: Array<Address>;
};

export function getReorderHooksInstructionDataEncoder(): Encoder<ReorderHooksInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["jobId", getU64Encoder()],
      ["action", getU8Encoder()],
      ["hooks", getArrayEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: REORDER_HOOKS_DISCRIMINATOR }),
  );
}

export function getReorderHooksInstructionDataDecoder(): Decoder<ReorderHooksInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["jobId", getU64Decoder()],
    ["action", getU8Decoder()],
    ["hooks", getArrayDecoder(getAddressDecoder())],
  ]);
}

export function getReorderHooksInstructionDataCodec(): Codec<
  ReorderHooksInstructionDataArgs,
  ReorderHooksInstructionData
> {
  return combineCodec(
    getReorderHooksInstructionDataEncoder(),
    getReorderHooksInstructionDataDecoder(),
  );
}

export type ReorderHooksAsyncInput<
  TAccountClient extends string = string,
  TAccountJob extends string = string,
  TAccountHookRouter extends string = string,
  TAccountRouterState extends string = string,
> = {
  client: TransactionSigner<TAccountClient>;
  job: Address<TAccountJob>;
  hookRouter?: Address<TAccountHookRouter>;
  routerState?: Address<TAccountRouterState>;
  jobId: ReorderHooksInstructionDataArgs["jobId"];
  action: ReorderHooksInstructionDataArgs["action"];
  hooks: ReorderHooksInstructionDataArgs["hooks"];
};

export async function getReorderHooksInstructionAsync<
  TAccountClient extends string,
  TAccountJob extends string,
  TAccountHookRouter extends string,
  TAccountRouterState extends string,
  TProgramAddress extends Address = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
>(
  input: ReorderHooksAsyncInput<
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ReorderHooksInstruction<
    TProgramAddress,
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MULTI_HOOK_ROUTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    client: { value: input.client ?? null, isWritable: false },
    job: { value: input.job ?? null, isWritable: false },
    hookRouter: { value: input.hookRouter ?? null, isWritable: true },
    routerState: { value: input.routerState ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hookRouter.value) {
    accounts.hookRouter.value = await findHookRouterPda({
      jobId: expectSome(args.jobId),
    });
  }
  if (!accounts.routerState.value) {
    accounts.routerState.value = await findRouterStatePda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.client),
      getAccountMeta(accounts.job),
      getAccountMeta(accounts.hookRouter),
      getAccountMeta(accounts.routerState),
    ],
    data: getReorderHooksInstructionDataEncoder().encode(
      args as ReorderHooksInstructionDataArgs,
    ),
    programAddress,
  } as ReorderHooksInstruction<
    TProgramAddress,
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState
  >);
}

export type ReorderHooksInput<
  TAccountClient extends string = string,
  TAccountJob extends string = string,
  TAccountHookRouter extends string = string,
  TAccountRouterState extends string = string,
> = {
  client: TransactionSigner<TAccountClient>;
  job: Address<TAccountJob>;
  hookRouter: Address<TAccountHookRouter>;
  routerState: Address<TAccountRouterState>;
  jobId: ReorderHooksInstructionDataArgs["jobId"];
  action: ReorderHooksInstructionDataArgs["action"];
  hooks: ReorderHooksInstructionDataArgs["hooks"];
};

export function getReorderHooksInstruction<
  TAccountClient extends string,
  TAccountJob extends string,
  TAccountHookRouter extends string,
  TAccountRouterState extends string,
  TProgramAddress extends Address = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
>(
  input: ReorderHooksInput<
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState
  >,
  config?: { programAddress?: TProgramAddress },
): ReorderHooksInstruction<
  TProgramAddress,
  TAccountClient,
  TAccountJob,
  TAccountHookRouter,
  TAccountRouterState
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MULTI_HOOK_ROUTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    client: { value: input.client ?? null, isWritable: false },
    job: { value: input.job ?? null, isWritable: false },
    hookRouter: { value: input.hookRouter ?? null, isWritable: true },
    routerState: { value: input.routerState ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.client),
      getAccountMeta(accounts.job),
      getAccountMeta(accounts.hookRouter),
      getAccountMeta(accounts.routerState),
    ],
    data: getReorderHooksInstructionDataEncoder().encode(
      args as ReorderHooksInstructionDataArgs,
    ),
    programAddress,
  } as ReorderHooksInstruction<
    TProgramAddress,
    TAccountClient,
    TAccountJob,
    TAccountHookRouter,
    TAccountRouterState
  >);
}

export type ParsedReorderHooksInstruction<
  TProgram extends string = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    client: TAccountMetas[0];
    job: TAccountMetas[1];
    hookRouter: TAccountMetas[2];
    routerState: TAccountMetas[3];
  };
  data: ReorderHooksInstructionData;
};

export function parseReorderHooksInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedReorderHooksInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      client: getNextAccount(),
      job: getNextAccount(),
      hookRouter: getNextAccount(),
      routerState: getNextAccount(),
    },
    data: getReorderHooksInstructionDataDecoder().decode(instruction.data),
  };
}
