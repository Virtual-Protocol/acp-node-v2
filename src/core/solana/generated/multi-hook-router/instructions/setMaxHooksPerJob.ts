/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findRouterStatePda } from "../pdas/index.js";
import { MULTI_HOOK_ROUTER_PROGRAM_ADDRESS } from "../programs/index.js";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared/index.js";

export const SET_MAX_HOOKS_PER_JOB_DISCRIMINATOR = new Uint8Array([
  211, 27, 171, 166, 105, 24, 236, 200,
]);

export function getSetMaxHooksPerJobDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_MAX_HOOKS_PER_JOB_DISCRIMINATOR,
  );
}

export type SetMaxHooksPerJobInstruction<
  TProgram extends string = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountRouterState extends string | AccountMeta<string> = string,
  TAccountAcpState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountRouterState extends string
        ? WritableAccount<TAccountRouterState>
        : TAccountRouterState,
      TAccountAcpState extends string
        ? ReadonlyAccount<TAccountAcpState>
        : TAccountAcpState,
      ...TRemainingAccounts,
    ]
  >;

export type SetMaxHooksPerJobInstructionData = {
  discriminator: ReadonlyUint8Array;
  newMax: number;
};

export type SetMaxHooksPerJobInstructionDataArgs = { newMax: number };

export function getSetMaxHooksPerJobInstructionDataEncoder(): FixedSizeEncoder<SetMaxHooksPerJobInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newMax", getU8Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_MAX_HOOKS_PER_JOB_DISCRIMINATOR,
    }),
  );
}

export function getSetMaxHooksPerJobInstructionDataDecoder(): FixedSizeDecoder<SetMaxHooksPerJobInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newMax", getU8Decoder()],
  ]);
}

export function getSetMaxHooksPerJobInstructionDataCodec(): FixedSizeCodec<
  SetMaxHooksPerJobInstructionDataArgs,
  SetMaxHooksPerJobInstructionData
> {
  return combineCodec(
    getSetMaxHooksPerJobInstructionDataEncoder(),
    getSetMaxHooksPerJobInstructionDataDecoder(),
  );
}

export type SetMaxHooksPerJobAsyncInput<
  TAccountAuthority extends string = string,
  TAccountRouterState extends string = string,
  TAccountAcpState extends string = string,
> = {
  /**
   * Must be the ACP core authority (`AcpState.authority`), verified in the
   * handler against `acp_state`. The router has no admin of its own.
   */
  authority: TransactionSigner<TAccountAuthority>;
  routerState?: Address<TAccountRouterState>;
  /** PDA address, owner, and stored `authority` are validated in the handler. */
  acpState: Address<TAccountAcpState>;
  newMax: SetMaxHooksPerJobInstructionDataArgs["newMax"];
};

export async function getSetMaxHooksPerJobInstructionAsync<
  TAccountAuthority extends string,
  TAccountRouterState extends string,
  TAccountAcpState extends string,
  TProgramAddress extends Address = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
>(
  input: SetMaxHooksPerJobAsyncInput<
    TAccountAuthority,
    TAccountRouterState,
    TAccountAcpState
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetMaxHooksPerJobInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountRouterState,
    TAccountAcpState
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MULTI_HOOK_ROUTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    routerState: { value: input.routerState ?? null, isWritable: true },
    acpState: { value: input.acpState ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.routerState.value) {
    accounts.routerState.value = await findRouterStatePda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.routerState),
      getAccountMeta(accounts.acpState),
    ],
    data: getSetMaxHooksPerJobInstructionDataEncoder().encode(
      args as SetMaxHooksPerJobInstructionDataArgs,
    ),
    programAddress,
  } as SetMaxHooksPerJobInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountRouterState,
    TAccountAcpState
  >);
}

export type SetMaxHooksPerJobInput<
  TAccountAuthority extends string = string,
  TAccountRouterState extends string = string,
  TAccountAcpState extends string = string,
> = {
  /**
   * Must be the ACP core authority (`AcpState.authority`), verified in the
   * handler against `acp_state`. The router has no admin of its own.
   */
  authority: TransactionSigner<TAccountAuthority>;
  routerState: Address<TAccountRouterState>;
  /** PDA address, owner, and stored `authority` are validated in the handler. */
  acpState: Address<TAccountAcpState>;
  newMax: SetMaxHooksPerJobInstructionDataArgs["newMax"];
};

export function getSetMaxHooksPerJobInstruction<
  TAccountAuthority extends string,
  TAccountRouterState extends string,
  TAccountAcpState extends string,
  TProgramAddress extends Address = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
>(
  input: SetMaxHooksPerJobInput<
    TAccountAuthority,
    TAccountRouterState,
    TAccountAcpState
  >,
  config?: { programAddress?: TProgramAddress },
): SetMaxHooksPerJobInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountRouterState,
  TAccountAcpState
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MULTI_HOOK_ROUTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    routerState: { value: input.routerState ?? null, isWritable: true },
    acpState: { value: input.acpState ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.routerState),
      getAccountMeta(accounts.acpState),
    ],
    data: getSetMaxHooksPerJobInstructionDataEncoder().encode(
      args as SetMaxHooksPerJobInstructionDataArgs,
    ),
    programAddress,
  } as SetMaxHooksPerJobInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountRouterState,
    TAccountAcpState
  >);
}

export type ParsedSetMaxHooksPerJobInstruction<
  TProgram extends string = typeof MULTI_HOOK_ROUTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Must be the ACP core authority (`AcpState.authority`), verified in the
     * handler against `acp_state`. The router has no admin of its own.
     */
    authority: TAccountMetas[0];
    routerState: TAccountMetas[1];
    /** PDA address, owner, and stored `authority` are validated in the handler. */
    acpState: TAccountMetas[2];
  };
  data: SetMaxHooksPerJobInstructionData;
};

export function parseSetMaxHooksPerJobInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetMaxHooksPerJobInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      routerState: getNextAccount(),
      acpState: getNextAccount(),
    },
    data: getSetMaxHooksPerJobInstructionDataDecoder().decode(instruction.data),
  };
}
