/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * Cached required-selectors bitmask for one configured sub-hook.
 *
 * Read via `validate_sub_hook` at config time and cached. Safe because
 * `HookMetadata.required_selectors` is write-once (set only in each hook's
 * `initialize`, no setter), so the cache can never diverge from the source.
 * If `HookMetadata` ever becomes mutable, `validate_completeness` must re-read
 * it live instead of trusting this cache.
 */
export type CachedHookMetadata = { hook: Address; requiredSelectors: number };

export type CachedHookMetadataArgs = CachedHookMetadata;

export function getCachedHookMetadataEncoder(): FixedSizeEncoder<CachedHookMetadataArgs> {
  return getStructEncoder([
    ["hook", getAddressEncoder()],
    ["requiredSelectors", getU8Encoder()],
  ]);
}

export function getCachedHookMetadataDecoder(): FixedSizeDecoder<CachedHookMetadata> {
  return getStructDecoder([
    ["hook", getAddressDecoder()],
    ["requiredSelectors", getU8Decoder()],
  ]);
}

export function getCachedHookMetadataCodec(): FixedSizeCodec<
  CachedHookMetadataArgs,
  CachedHookMetadata
> {
  return combineCodec(
    getCachedHookMetadataEncoder(),
    getCachedHookMetadataDecoder(),
  );
}
