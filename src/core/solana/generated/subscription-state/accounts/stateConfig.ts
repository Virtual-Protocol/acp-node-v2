/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const STATE_CONFIG_DISCRIMINATOR = new Uint8Array([
  105, 124, 131, 88, 127, 32, 75, 16,
]);

export function getStateConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    STATE_CONFIG_DISCRIMINATOR,
  );
}

export type StateConfig = {
  discriminator: ReadonlyUint8Array;
  /** Current program authority; can add/remove writers and transfer ownership. */
  authority: Address;
  /** Nominated successor (two-step transfer). `None` until `nominate_authority` is called. */
  pendingAuthority: Option<Address>;
  /** PDA bump seed. */
  bump: number;
};

export type StateConfigArgs = {
  /** Current program authority; can add/remove writers and transfer ownership. */
  authority: Address;
  /** Nominated successor (two-step transfer). `None` until `nominate_authority` is called. */
  pendingAuthority: OptionOrNullable<Address>;
  /** PDA bump seed. */
  bump: number;
};

/** Gets the encoder for {@link StateConfigArgs} account data. */
export function getStateConfigEncoder(): Encoder<StateConfigArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["pendingAuthority", getOptionEncoder(getAddressEncoder())],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: STATE_CONFIG_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link StateConfig} account data. */
export function getStateConfigDecoder(): Decoder<StateConfig> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["pendingAuthority", getOptionDecoder(getAddressDecoder())],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link StateConfig} account data. */
export function getStateConfigCodec(): Codec<StateConfigArgs, StateConfig> {
  return combineCodec(getStateConfigEncoder(), getStateConfigDecoder());
}

export function decodeStateConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<StateConfig, TAddress>;
export function decodeStateConfig<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<StateConfig, TAddress>;
export function decodeStateConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<StateConfig, TAddress> | MaybeAccount<StateConfig, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getStateConfigDecoder(),
  );
}

export async function fetchStateConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<StateConfig, TAddress>> {
  const maybeAccount = await fetchMaybeStateConfig(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeStateConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<StateConfig, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeStateConfig(maybeAccount);
}

export async function fetchAllStateConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<StateConfig>[]> {
  const maybeAccounts = await fetchAllMaybeStateConfig(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeStateConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<StateConfig>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeStateConfig(maybeAccount));
}
