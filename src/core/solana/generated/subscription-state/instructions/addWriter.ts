/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findStateConfigPda, findWriterRegistryPda } from "../pdas/index.js";
import { SUBSCRIPTION_STATE_PROGRAM_ADDRESS } from "../programs/index.js";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared/index.js";

export const ADD_WRITER_DISCRIMINATOR = new Uint8Array([
  95, 14, 80, 179, 133, 216, 154, 108,
]);

export function getAddWriterDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ADD_WRITER_DISCRIMINATOR);
}

export type AddWriterInstruction<
  TProgram extends string = typeof SUBSCRIPTION_STATE_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountStateConfig extends string | AccountMeta<string> = string,
  TAccountWriterRegistry extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountStateConfig extends string
        ? ReadonlyAccount<TAccountStateConfig>
        : TAccountStateConfig,
      TAccountWriterRegistry extends string
        ? WritableAccount<TAccountWriterRegistry>
        : TAccountWriterRegistry,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddWriterInstructionData = {
  discriminator: ReadonlyUint8Array;
  writer: Address;
};

export type AddWriterInstructionDataArgs = { writer: Address };

export function getAddWriterInstructionDataEncoder(): FixedSizeEncoder<AddWriterInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["writer", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: ADD_WRITER_DISCRIMINATOR }),
  );
}

export function getAddWriterInstructionDataDecoder(): FixedSizeDecoder<AddWriterInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["writer", getAddressDecoder()],
  ]);
}

export function getAddWriterInstructionDataCodec(): FixedSizeCodec<
  AddWriterInstructionDataArgs,
  AddWriterInstructionData
> {
  return combineCodec(
    getAddWriterInstructionDataEncoder(),
    getAddWriterInstructionDataDecoder(),
  );
}

export type AddWriterAsyncInput<
  TAccountAuthority extends string = string,
  TAccountStateConfig extends string = string,
  TAccountWriterRegistry extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  stateConfig?: Address<TAccountStateConfig>;
  writerRegistry?: Address<TAccountWriterRegistry>;
  systemProgram?: Address<TAccountSystemProgram>;
  writer: AddWriterInstructionDataArgs["writer"];
};

export async function getAddWriterInstructionAsync<
  TAccountAuthority extends string,
  TAccountStateConfig extends string,
  TAccountWriterRegistry extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SUBSCRIPTION_STATE_PROGRAM_ADDRESS,
>(
  input: AddWriterAsyncInput<
    TAccountAuthority,
    TAccountStateConfig,
    TAccountWriterRegistry,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AddWriterInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountStateConfig,
    TAccountWriterRegistry,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SUBSCRIPTION_STATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    stateConfig: { value: input.stateConfig ?? null, isWritable: false },
    writerRegistry: { value: input.writerRegistry ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.stateConfig.value) {
    accounts.stateConfig.value = await findStateConfigPda();
  }
  if (!accounts.writerRegistry.value) {
    accounts.writerRegistry.value = await findWriterRegistryPda({
      writer: expectSome(args.writer),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.stateConfig),
      getAccountMeta(accounts.writerRegistry),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAddWriterInstructionDataEncoder().encode(
      args as AddWriterInstructionDataArgs,
    ),
    programAddress,
  } as AddWriterInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountStateConfig,
    TAccountWriterRegistry,
    TAccountSystemProgram
  >);
}

export type AddWriterInput<
  TAccountAuthority extends string = string,
  TAccountStateConfig extends string = string,
  TAccountWriterRegistry extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  stateConfig: Address<TAccountStateConfig>;
  writerRegistry: Address<TAccountWriterRegistry>;
  systemProgram?: Address<TAccountSystemProgram>;
  writer: AddWriterInstructionDataArgs["writer"];
};

export function getAddWriterInstruction<
  TAccountAuthority extends string,
  TAccountStateConfig extends string,
  TAccountWriterRegistry extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SUBSCRIPTION_STATE_PROGRAM_ADDRESS,
>(
  input: AddWriterInput<
    TAccountAuthority,
    TAccountStateConfig,
    TAccountWriterRegistry,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AddWriterInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountStateConfig,
  TAccountWriterRegistry,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SUBSCRIPTION_STATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    stateConfig: { value: input.stateConfig ?? null, isWritable: false },
    writerRegistry: { value: input.writerRegistry ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.stateConfig),
      getAccountMeta(accounts.writerRegistry),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAddWriterInstructionDataEncoder().encode(
      args as AddWriterInstructionDataArgs,
    ),
    programAddress,
  } as AddWriterInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountStateConfig,
    TAccountWriterRegistry,
    TAccountSystemProgram
  >);
}

export type ParsedAddWriterInstruction<
  TProgram extends string = typeof SUBSCRIPTION_STATE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    stateConfig: TAccountMetas[1];
    writerRegistry: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: AddWriterInstructionData;
};

export function parseAddWriterInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddWriterInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      stateConfig: getNextAccount(),
      writerRegistry: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddWriterInstructionDataDecoder().decode(instruction.data),
  };
}
