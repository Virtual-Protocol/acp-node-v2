/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findStateConfigPda, findWriterRegistryPda } from "../pdas/index.js";
import { SUBSCRIPTION_STATE_PROGRAM_ADDRESS } from "../programs/index.js";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared/index.js";

export const REMOVE_WRITER_DISCRIMINATOR = new Uint8Array([
  247, 145, 28, 134, 227, 132, 243, 83,
]);

export function getRemoveWriterDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REMOVE_WRITER_DISCRIMINATOR,
  );
}

export type RemoveWriterInstruction<
  TProgram extends string = typeof SUBSCRIPTION_STATE_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountStateConfig extends string | AccountMeta<string> = string,
  TAccountWriterRegistry extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountStateConfig extends string
        ? ReadonlyAccount<TAccountStateConfig>
        : TAccountStateConfig,
      TAccountWriterRegistry extends string
        ? WritableAccount<TAccountWriterRegistry>
        : TAccountWriterRegistry,
      ...TRemainingAccounts,
    ]
  >;

export type RemoveWriterInstructionData = {
  discriminator: ReadonlyUint8Array;
  writer: Address;
};

export type RemoveWriterInstructionDataArgs = { writer: Address };

export function getRemoveWriterInstructionDataEncoder(): FixedSizeEncoder<RemoveWriterInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["writer", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: REMOVE_WRITER_DISCRIMINATOR }),
  );
}

export function getRemoveWriterInstructionDataDecoder(): FixedSizeDecoder<RemoveWriterInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["writer", getAddressDecoder()],
  ]);
}

export function getRemoveWriterInstructionDataCodec(): FixedSizeCodec<
  RemoveWriterInstructionDataArgs,
  RemoveWriterInstructionData
> {
  return combineCodec(
    getRemoveWriterInstructionDataEncoder(),
    getRemoveWriterInstructionDataDecoder(),
  );
}

export type RemoveWriterAsyncInput<
  TAccountAuthority extends string = string,
  TAccountStateConfig extends string = string,
  TAccountWriterRegistry extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  stateConfig?: Address<TAccountStateConfig>;
  writerRegistry?: Address<TAccountWriterRegistry>;
  writer: RemoveWriterInstructionDataArgs["writer"];
};

export async function getRemoveWriterInstructionAsync<
  TAccountAuthority extends string,
  TAccountStateConfig extends string,
  TAccountWriterRegistry extends string,
  TProgramAddress extends Address = typeof SUBSCRIPTION_STATE_PROGRAM_ADDRESS,
>(
  input: RemoveWriterAsyncInput<
    TAccountAuthority,
    TAccountStateConfig,
    TAccountWriterRegistry
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RemoveWriterInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountStateConfig,
    TAccountWriterRegistry
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SUBSCRIPTION_STATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    stateConfig: { value: input.stateConfig ?? null, isWritable: false },
    writerRegistry: { value: input.writerRegistry ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.stateConfig.value) {
    accounts.stateConfig.value = await findStateConfigPda();
  }
  if (!accounts.writerRegistry.value) {
    accounts.writerRegistry.value = await findWriterRegistryPda({
      writer: expectSome(args.writer),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.stateConfig),
      getAccountMeta(accounts.writerRegistry),
    ],
    data: getRemoveWriterInstructionDataEncoder().encode(
      args as RemoveWriterInstructionDataArgs,
    ),
    programAddress,
  } as RemoveWriterInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountStateConfig,
    TAccountWriterRegistry
  >);
}

export type RemoveWriterInput<
  TAccountAuthority extends string = string,
  TAccountStateConfig extends string = string,
  TAccountWriterRegistry extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  stateConfig: Address<TAccountStateConfig>;
  writerRegistry: Address<TAccountWriterRegistry>;
  writer: RemoveWriterInstructionDataArgs["writer"];
};

export function getRemoveWriterInstruction<
  TAccountAuthority extends string,
  TAccountStateConfig extends string,
  TAccountWriterRegistry extends string,
  TProgramAddress extends Address = typeof SUBSCRIPTION_STATE_PROGRAM_ADDRESS,
>(
  input: RemoveWriterInput<
    TAccountAuthority,
    TAccountStateConfig,
    TAccountWriterRegistry
  >,
  config?: { programAddress?: TProgramAddress },
): RemoveWriterInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountStateConfig,
  TAccountWriterRegistry
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SUBSCRIPTION_STATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    stateConfig: { value: input.stateConfig ?? null, isWritable: false },
    writerRegistry: { value: input.writerRegistry ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.stateConfig),
      getAccountMeta(accounts.writerRegistry),
    ],
    data: getRemoveWriterInstructionDataEncoder().encode(
      args as RemoveWriterInstructionDataArgs,
    ),
    programAddress,
  } as RemoveWriterInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountStateConfig,
    TAccountWriterRegistry
  >);
}

export type ParsedRemoveWriterInstruction<
  TProgram extends string = typeof SUBSCRIPTION_STATE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    stateConfig: TAccountMetas[1];
    writerRegistry: TAccountMetas[2];
  };
  data: RemoveWriterInstructionData;
};

export function parseRemoveWriterInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRemoveWriterInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      stateConfig: getNextAccount(),
      writerRegistry: getNextAccount(),
    },
    data: getRemoveWriterInstructionDataDecoder().decode(instruction.data),
  };
}
